- name: loops
  hosts: localhost
  tasks:
  - name: print persons
    ansible.builtin.debug:
      msg: "Hello {{item}}" # item is a default keyword
      state: prsent
    loop:
    - git
    - mysql
    - postfix
    